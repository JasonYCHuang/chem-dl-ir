# chem-dl-ir training

This folder contains trainging for the deep learning model.

### 1. Training a deep learning model

We use pytorch to build the model.

Then, please follow steps in `training_spectrum_to_fgs/INSTALL*.md`.

